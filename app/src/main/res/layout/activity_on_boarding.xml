<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.karrar.movieapp.ui.onboarding.OnBoardingViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background_color_screen"
        tools:context=".ui.onboarding.OnBoardingActivity">

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPager"
            android:layout_width="@dimen/zero_dp"
            android:layout_height="@dimen/zero_dp"
            app:layout_constraintBottom_toTopOf="@+id/bottomCard"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.cardview.widget.CardView
            android:id="@+id/bottomCard"
            android:layout_width="@dimen/zero_dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/spacing_medium"
            android:layout_marginBottom="@dimen/spacing_medium"
            app:cardBackgroundColor="@color/background_color_bottomSheet"
            app:cardCornerRadius="@dimen/radius_cineVerse_size3xl"
            app:cardElevation="@dimen/zero_dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/spacing_large">

                <TextView
                    android:id="@+id/titleText"
                    android:layout_width="@dimen/zero_dp"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_small"
                    android:gravity="center"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    tools:text="@string/welcome_to_your_movie_universe"
                    style="@style/TextStyle.Title.Medium" />

                <TextView
                    android:id="@+id/descriptionText"
                    android:layout_width="@dimen/zero_dp"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_small"
                    android:gravity="center"
                    android:textColor="@color/shade_color_secondary"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@+id/titleText"
                    tools:text="@string/discover_track_and_rate_your_favorite_movies_and_series"
                    style="@style/TextStyle.Body.Medium.Medium" />

                <androidx.constraintlayout.widget.ConstraintLayout
                    android:id="@+id/buttonContainer"
                    android:layout_width="@dimen/zero_dp"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_large"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@+id/descriptionText">

                    <androidx.appcompat.widget.AppCompatImageButton
                        android:id="@+id/previousButton"
                        android:layout_width="@dimen/on_boarding_btn_size"
                        android:layout_height="@dimen/on_boarding_btn_size"
                        android:src="@drawable/ic_left_arrow"
                        android:background="@drawable/left_on_boarding_back_btn"
                        android:tint="@color/shade_color_primary"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintTop_toTopOf="parent" />

                    <androidx.appcompat.widget.AppCompatImageButton
                        android:id="@+id/nextButton"
                        android:layout_width="@dimen/on_boarding_btn_size"
                        android:layout_height="@dimen/on_boarding_btn_size"
                        android:background="@drawable/right_on_boarding_forward_btn"
                        android:src="@drawable/ic_right_arrow"
                        android:tint="@color/button_color_onPrimary"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toTopOf="parent" />

                    <androidx.appcompat.widget.AppCompatButton
                        android:id="@+id/getStartedButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:background="@drawable/right_on_boarding_forward_btn"
                        android:paddingStart="@dimen/spacing_large"
                        android:paddingEnd="@dimen/spacing_medium"
                        android:paddingVertical="@dimen/vertical_btn_padding"
                        android:text="@string/get_started"
                        android:textAllCaps="false"
                        style="@style/TextStyle.Body.Medium.Medium"
                        android:textColor="@color/button_color_onPrimary"
                        android:visibility="gone"
                        android:drawableEnd="@drawable/ic_right_arrow"
                        android:drawableTint="@color/button_color_onPrimary"
                        android:drawablePadding="@dimen/spacing_small"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        tools:visibility="visible" />

                </androidx.constraintlayout.widget.ConstraintLayout>

            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.cardview.widget.CardView>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>